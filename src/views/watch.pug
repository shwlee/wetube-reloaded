extends base.pug
include mixins/video

block content    
    div
        p=video.description
        small=video.createdAt
    a(href=`${id}/edit`) Edit Video &rarr;
    +videoContext(video)
            