extends base.pug
include mixins/videos

block content 
    form(method="GET").search__form
        input(type="text", placeholder="Search by title", name="keyword")
        button 
            i.fas.fa-search        
    div.video-grid
        +videoContexts(videos)
        