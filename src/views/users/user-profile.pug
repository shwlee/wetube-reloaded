extends ../base
include ../mixins/videos

block content 
    div.profile__data   
        img(src=`/users/store/${user.avatarFile}`).avatar
        h3=user.name
    div.video-grid
        +videoContexts(user.videos)